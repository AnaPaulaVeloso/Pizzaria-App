import { StyleSheet } from 'react-native';

const avatarStyles = StyleSheet.create({
  avatar: {
    width: 32,
    height: 32,
    borderRadius: 16,
    position: 'absolute',
    top: 16,
    right: 16,
    zIndex: 50,
  },
});

export default avatarStyles; 